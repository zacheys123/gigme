export const setStates = {
  profile: (setNavStates) => {
    setNavStates(true);
  },
  dashboard: (setNavStates) => {
    setNavStates(true);
  },
  posts: (setNavStates) => {
    setNavStates(true);
  },
  gigs: (setNavStates) => {
    setNavStates(true);
  },
  settings: (setNavStates) => {
    setNavStates(true);
  },
  logout: (setNavStates) => {
    setNavStates(true);
  },
};
