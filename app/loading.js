import { CircularProgress } from "@mui/material";

export default function Loading() {
  return (
    <div className="h-[100vh] w-full flex justify-center items-center ">
      <h6 className="text-red-500 font-bold"> loading page...</h6>
    </div>
  );
}
